#include<iostream>
using namespace std;
int main(){
    int length;
    int breadth;
    cout<<"Enter the length of rectangle:";
    cin>>length;
    cout<<"Enter the breadth of rectangle:";
    cin>>breadth;
    cout<<"Area of rectangle:";
    cout<<length*breadth;
    cout<<endl;
    cout<<"Perimeter of rectangle:";
    cout<<2*(length+breadth);   
    if(Area of rectangle > Perimeter of rectangle);
     cout<<"area wins";

}